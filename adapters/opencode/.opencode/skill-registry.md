# Skill Registry

_Auto-generated by /registry-refresh — do not edit manually._  
_Last updated: 2026-06-03_

## Skills

| Command | Description | When to use |
|---|---|---|
| `/checkpoint` | Save a structured context snapshot to short-term memory so work can be recovered after context compaction. | Before likely compaction in a long session |
| `/doctor` | Verify the full DakoHarness installation — storage backend, .env, the logging plugin, both MCPs, and binaries. | After install or when something is broken |
| `/memory-audit` | Audit long-term memories — deduplicate, flag stale (90+ days), resolve contradictions. | Periodic memory hygiene pass |
| `/promote` | Promote a short-term pattern to long-term memory. | Pattern has proven durable across sessions |
| `/promote-team` | Promote a project memory to team scope, searchable across all projects. | Lesson is broadly applicable beyond one project |
| `/recall` | Search long-term memory with query expansion and merged variants. | Need a past decision or convention |
| `/recall-session` | Search conversation history with semantic recall. | Recover a specific earlier exchange |
| `/session-end` | End-of-session cleanup — promote valuable patterns and leave a clean state. | Wrapping up a working session |
| `/setup` | First-time project setup — backend, .env, opencode.json, plugin, AGENTS.md. | Once per new project |
| `/wi-start` | Start a new workitem — intake, routing, and full workflow. | Beginning a traceable feature/refactor |
| `/wi-next` | Advance the active workitem to the next phase. | Moving a workitem forward |
| `/wi-status` | Show the current state of the active workitem. | Checking where a workitem stands |
| `/wi-intake` | Run or re-run the intake phase on a workitem. | Capturing initial workitem context |
| `/wi-analyze` | Interview the user and produce signed-off acceptance criteria. | Eliciting requirements |
| `/wi-propose` | Generate approaches, surface trade-offs, record the selected direction. | Multiple viable directions exist |
| `/wi-plan` | Explore the codebase and produce a sequenced implementation plan. | Planning implementation against ACs |
| `/wi-implement` | Dispatch the wi-implementer sub-agent for the implement phase. | Executing an approved plan |
| `/wi-review` | Verify every AC and plan step against the implementation. | Reviewing completed implementation |
| `/wi-document` | Update project docs and write the workitem documentation record. | After implementation passes review |
| `/wi-repo` | Suggest a commit message; never touch git without approval. | Preparing repo actions |
| `/wi-archive` | Archive the completed workitem to long-term memory. | Closing out a finished workitem |
| `/wi-park` | Pause the active workitem indefinitely without cancelling. | Setting work aside temporarily |
| `/wi-cancel` | Cancel the active workitem or current phase; files kept. | Abandoning a workitem |
